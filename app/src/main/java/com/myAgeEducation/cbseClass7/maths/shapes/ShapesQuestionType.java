package com.myAgeEducation.cbseClass7.maths.shapes;

public enum ShapesQuestionType {
    POLYGON_3_SIDES,           // A polygon with 3 line segments is called a ________
    POLYGON_4_SIDES,           // A polygon with 4 line segments is called a ________
    RECTANGLE_DEFINITION,      // A _______ is a special quadrilateral in which the opposite sides are of equal length
    SQUARE_DEFINITION,         // A _______ is a special rectangle in which all four sides are equal
    SQUARE_QUADRILATERAL_TF,   // All squares are quadrilaterals (TRUE/FALSE)
    TRIANGLE_POLYGON_TF,       // A triangle is a polygon
    POLYGON_SEGMENTS_TF,       // A polygon is made up of line segments
    QUADRILATERAL_RECTANGLE_TF, // All quadrilaterals are rectangles
    DIAMETER_RADIUS_RELATION,  // The _______ is twice the length of the radius
    RADII_EQUAL_TF,            // All the radii of a particular circle are of equal length
    DIAMETER_HALF_RADIUS_TF,   // The diameter is half the radius
    DIAMETER_CENTER_TF,        // The diameter goes through the center of the circle
    CALCULATE_RADIUS,          // What is the radius of a circle whose diameter is 70 cm
    CALCULATE_DIAMETER,        // What is the diameter of a circle whose radius is 14 cm
    CLOSED_CURVES_POLYGON_TF,  // All closed curves form polygons
    CIRCUMFERENCE_DEFINITION_TF,// The circumference of a circle is its length
    RADIUS_DIAMETER_CENTER_TF, // The radius and diameter of a circle meet at the center of the circle
    OTHER_RADII_LENGTH,        // If the length of a radius of a cicle is 5 cm, what is the length of the other radii...
    RADIUS_HALF_DIAMETER_TF,   // The radius is half the length of a diameter of a circle.
    SUM_RADII_DIAMETER_TF,      // The sum of the length of 2 radii of a cicle is equal to the length of its diameter
    SYMMETRY_CONCEPT            // Questions about lines of symmetry
}

